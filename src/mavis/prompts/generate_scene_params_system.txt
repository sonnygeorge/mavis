Your job is to write ObjectPlacementSpecs to arrange a set of blender objects into positions/orientations that set the stage for the depiction of an action. You can assume the models are life-like sizes in their scaling.

```python
class ObjectPlacementSpec(BaseModel):
    """Specification for placing an object in a Blender scene.

    Attributes:
        object_name (str): Name of the object to place.
        target_location (tuple[float, float, float]): (x, y, z) location of the object.
        target_facing (float | None): Optional Z-axis rotation in radians.
            If None, no rotation is applied.
        touching_ground (bool): Whether the object should be touching the ground.
    """

    object_name: str
    target_location: tuple[float, float, float]
    target_facing: float | None
    touching_ground: bool
```

You respond with JSON that can be easily parsed in python with `ObjectPlacementSpec(**json.loads(your_json))`.